[package]
name = "graphql-static-analysis-benchmark"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
apollo-compiler = "1.32.0"
graphql-static-analysis = { path = ".." }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
